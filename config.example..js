const credentials = {
  user: "EXAMPLE_USER",
  host: "EXAMPLE_HOST",
  database: "EXAMPLE_DATABASE",
  password: "EXAMPLE_PASSWORD",
  port: "EXAMPLE_PORT",
};

module.exports = credentials;

// Make a copy of this file and rename it to config.js.
// Change all configurations to match your PostgreSQL credentials.